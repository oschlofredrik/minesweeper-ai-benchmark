{
  "name": "@tilts/web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'Static files - no build needed'",
    "lint": "echo 'TODO: Add linter for JS files'"
  },
  "dependencies": {
    "@tilts/shared": "workspace:*"
  }
}